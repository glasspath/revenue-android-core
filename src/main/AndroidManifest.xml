<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- SEE PRIVACY_POLICY.md! Location, used by GPS mileage tracking -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <!-- SEE PRIVACY_POLICY.md! Contacts and Calls, can be synced with Revenue desktop App -->
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.READ_CALL_LOG" />

    <!-- SEE PRIVACY_POLICY.md! Camera, used for QR code scanning for syncing with Revenue desktop App -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- SEE PRIVACY_POLICY.md! Networking, used for syncing with Revenue desktop App -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />

    <!-- Storage, used for writing sync file for importing with Revenue desktop App -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <!-- Service, used for updating active registrations when App is closed -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />

    <!-- Vibrate, used for feedback when pressing widget buttons -->
    <uses-permission android:name="android.permission.VIBRATE"/>

    <!-- Notifications, used for informing the user about active registrations -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

</manifest>